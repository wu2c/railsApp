<%= form_for(@newsarticle) do |f| %>
  <% if @newsarticle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@newsarticle.errors.count, "error") %> prohibited this newsarticle from being saved:</h2>

      <ul>
      <% @newsarticle.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :article %><br />
    <%= f.text_area :article %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
